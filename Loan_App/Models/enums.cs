namespace Loan_App.Models
{
    public enum LoanState
    {
        PENDING,
        APPROVED,
        PAID,
        REJECT
    }

    public enum RepaymentStatus
    {
        PENDING,
        PAID
    }
}
